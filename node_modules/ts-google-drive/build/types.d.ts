import { Credentials, GoogleAuthOptions, OAuth2ClientOptions } from "google-auth-library";
export declare type ITsGoogleDriveOptions = GoogleAuthOptions | {
    oAuthCredentials: Credentials;
    oauthClientOptions?: OAuth2ClientOptions;
};
export declare type IUpdateMetaOptions = {
    name?: string;
    parent?: string;
    description?: string;
};
export declare type ICreateFolderOptions = {
    name?: string;
    parent?: string;
    description?: string;
};
