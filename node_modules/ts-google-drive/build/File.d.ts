/// <reference types="node" />
import { BaseExternalAccountClient, OAuth2Client } from "google-auth-library";
import { IUpdateMetaOptions } from "./types";
export declare class File {
    client: OAuth2Client | BaseExternalAccountClient;
    id: string;
    name: string;
    mimeType: string;
    kind: string;
    modifiedTime: string;
    createdTime: string;
    size: number;
    parents: string[];
    constructor(client: OAuth2Client | BaseExternalAccountClient);
    get modifiedAt(): Date;
    get createdAt(): Date;
    get isFolder(): boolean;
    download(): Promise<Buffer>;
    createStream(): Promise<Buffer>;
    update(options?: IUpdateMetaOptions): Promise<void>;
    delete(): Promise<boolean>;
    private _getClient;
}
